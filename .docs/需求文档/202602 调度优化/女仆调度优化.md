
## 女仆调度优化

### 任务总览

+ 优化代码结构，包括ts化全部文件，从女仆本体开始，然后覆盖周边模块
+ 将json执行的决策和动作（如播放音效）等尽量移到脚本侧

### 优化女仆脚本侧代码

路径：`TouHouLittleMaid_BP/typescripts/src/maid`

分为以下几个模块：

基础层：
+ 女仆实体接口：提供对女仆的信息读取和修改接口
  + 模型：实体接口的一个分支，提供对模型的修改
  + 字符化与字符解析：负责女仆实体和字符串的转换（目前的StrMaid）
+ 女仆事件：女仆事件的入口，包括交互、生成、心跳等

第二层：
+ 女仆UI：负责展示表单
+ 工作：负责农作等任务调度